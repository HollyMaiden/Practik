
&НаКлиенте
Процедура КоличествоПриИзменении(Элемент)
	Если ЗначениеЗаполнено(Объект.Цена) И ЗначениеЗаполнено(Объект.Количество) Тогда
		Объект.ОбщаяСумма = Объект.Цена*Объект.Количество;
	КонецЕсли;
КонецПроцедуры
